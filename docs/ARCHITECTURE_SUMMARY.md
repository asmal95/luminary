# Архитектурное резюме проекта Luminary

## 📋 Обзор

Этот документ суммирует все принятые архитектурные решения для проекта AI-ревьюера кода.

**Дата создания:** 2026
**Статус:** Утверждено для MVP разработки

---

## 🏗️ Архитектурные решения

### 1. Общая архитектура (ADR-0001)
- **Многослойная архитектура**: Presentation → Application → Domain → Infrastructure
- **Разделение ответственности**: четкие границы между слоями
- **Расширяемость**: возможность замены реализации без изменения интерфейсов

### 2. Обработка файлов (ADR-0002)
- **Последовательная обработка** для MVP (с возможностью миграции на параллельную)
- **Группировка изменений** в файле в один запрос к LLM
- **Retry стратегия**: экспоненциальный backoff, максимум 3 попытки

### 3. LLM провайдеры (ADR-0003)
- **Паттерн Strategy + Factory** для поддержки множественных провайдеров
- **Приоритет #1**: OpenRouter API
- **Дополнительно**: OpenAI, DeepSeek, VLLM (offline модели)
- **Единый интерфейс** для всех провайдеров

### 4. Валидация комментариев (ADR-0004)
- **Конфигурируемая LLM** для валидации (может быть та же или легче)
- **Критерии**: релевантность, полезность, не избыточность
- **Обработка**: отклонение невалидных с логированием для статистики

### 5. Управление контекстом (ADR-0005)
- **По умолчанию**: весь файл + изменения в одном запросе
- **Chunking** для файлов выше лимита контекста (с перекрытием)
- **Настраиваемые лимиты** через конфигурацию

### 6. Технический стек (ADR-0006)
- **GitLab**: `python-gitlab` (официальная библиотека)
- **Шаблонизация**: f-strings для MVP (возможна миграция на Jinja2)
- **HTTP**: `requests` (синхронный, с возможностью миграции на async)
- **CLI**: `click`
- **Конфигурация**: `pyyaml`

### 7. Конфигурация (ADR-0007)
- **Иерархия**: дефолт → `.ai-reviewer.yml` → CLI аргументы
- **Настраиваемые параметры**: provider, model, temperature, max_tokens, top_p
- **Промпты**: дефолтные встроены, возможность переопределения через конфиг

### 8. Крайние случаи (ADR-0008)
- **Бинарные файлы**: автоматический пропуск
- **Игнорирование**: настраиваемые паттерны + дефолтные
- **Лимиты MR**: настраиваемые, по умолчанию без лимитов
- **При превышении**: частичный анализ с предупреждением

### 9. Формат комментариев (ADR-0009)
- **Режимы**: inline, summary, both (выбор через CLI/конфиг)
- **Markdown**: полная поддержка форматирования
- **Severity levels**: info, warning, error

---

## 🔧 Ключевые технические решения

### Обработка файлов
```
MR → Парсинг diff → Фильтрация → Последовательная обработка → Валидация → Отправка
```

### LLM провайдеры
```
AbstractLLMProvider (interface)
├── OpenRouterProvider
├── OpenAIProvider
├── DeepSeekProvider
└── VLLMProvider
```

### Валидация
```
Сгенерированный комментарий → LLM валидатор → Критерии оценки → Решение (отправить/отклонить)
```

---

## 📁 Структура проекта (предварительная)

```
luminary/
├── src/
│   ├── presentation/      # CLI интерфейс
│   ├── application/       # Оркестрация
│   ├── domain/            # Бизнес-логика
│   │   ├── models/        # Сущности (FileChange, Comment, etc.)
│   │   ├── services/      # Сервисы (ReviewService, ValidatorService)
│   │   └── providers/     # Интерфейсы провайдеров
│   └── infrastructure/    # Внешние интеграции
│       ├── gitlab/        # GitLab client
│       └── llm/           # LLM провайдеры
├── config/                # Дефолтные конфиги и промпты
├── tests/                 # Тесты
├── docs/                  # Документация
│   └── ADR/              # Архитектурные решения
└── ROADMAP.md            # План разработки
```

---

## 🎯 Критерии успеха MVP

1. ✅ Полный цикл: CLI → GitLab MR → анализ → комментарии
2. ✅ Поддержка OpenRouter (минимум + еще один провайдер)
3. ✅ Валидация комментариев работает
4. ✅ Работает в GitLab CI через Docker
5. ✅ Конфигурируется через YAML и CLI
6. ✅ Обрабатывает типичные MR (до 20-30 файлов)

---

## 📊 Оценка времени разработки

- **Этап 1**: Прототип ядра - 3-5 дней
- **Этап 2**: Интеграция LLM и валидация - 5-7 дней
- **Этап 3**: Полноценная обработка MR - 7-10 дней
- **Этап 4**: Дополнительные провайдеры - 5-7 дней
- **Этап 5**: Оптимизация и упаковка - 5-7 дней

**Итого MVP: 25-36 дней** (5-7 недель)

---

## 🔄 Миграционные пути

### Параллельная обработка
- Добавить пул потоков на уровне Application Layer
- Сохранить интерфейсы Domain Layer без изменений

### Асинхронность
- Заменить синхронные вызовы на async в Infrastructure Layer
- Domain Layer остается без изменений

### Jinja2 для промптов
- Заменить f-strings на Jinja2 шаблоны
- Сохранить обратную совместимость с простыми промптами

---

## 📚 Дополнительные документы

- [Roadmap](ROADMAP.md) - детальный план разработки
- [ADR документы](ADR/) - детальные архитектурные решения
- [Рекомендации по валидации](validation-strategy-recommendations.md) - стратегия валидации

---

## ✅ Следующие шаги

1. Утверждение архитектуры и Roadmap
2. Начало Этапа 1: Прототип ядра
3. Создание структуры проекта
4. Реализация базовых моделей и интерфейсов

---

**Статус:** Готово к началу разработки ✅
