# Архитектурное резюме проекта Luminary

## 📋 Обзор проекта

**Luminary** — это AI-ревьюер кода для автоматического анализа Merge Requests в GitLab. Проект использует LLM (Large Language Models) для генерации конструктивных комментариев к коду, валидации этих комментариев и автоматической отправки их в GitLab.

**Текущий статус:** MVP завершен (Этапы 1-4). Проект функционален и готов к использованию.

**Версия:** 0.1.0

---

## 🏗️ Архитектура

### Многослойная архитектура

Проект следует принципам Clean Architecture с четким разделением ответственности:

```
┌─────────────────────────────────────┐
│   Presentation Layer (CLI)          │  ← CLI интерфейс (click)
├─────────────────────────────────────┤
│   Application Layer (Orchestration)   │  ← ReviewService, MRReviewService
├─────────────────────────────────────┤
│   Domain Layer (Business Logic)      │  ← Models, Validators, Prompts
├─────────────────────────────────────┤
│   Infrastructure Layer (External)    │  ← GitLab, LLM, Config, HTTP
└─────────────────────────────────────┘
```

### Структура проекта

```
luminary/
├── src/luminary/
│   ├── domain/              # Бизнес-логика
│   │   ├── models/          # FileChange, Comment, ReviewResult
│   │   ├── prompts/         # ReviewPromptBuilder, ValidationPromptBuilder
│   │   └── validators/      # CommentValidator
│   ├── application/         # Оркестрация
│   │   ├── review_service.py      # Ревью одного файла
│   │   └── mr_review_service.py    # Ревью всего MR
│   ├── infrastructure/      # Внешние интеграции
│   │   ├── llm/             # LLM провайдеры (OpenAI, OpenRouter, DeepSeek, VLLM, Mock)
│   │   ├── gitlab/          # GitLabClient
│   │   ├── config/          # ConfigManager
│   │   ├── http_client.py   # HTTP клиент с retry
│   │   ├── diff_parser.py   # Парсинг diff
│   │   └── file_filter.py   # Фильтрация файлов
│   └── cli.py               # CLI интерфейс
├── tests/                   # Тесты
├── docs/                    # Документация
│   └── ADR/                 # Архитектурные решения
└── examples/                # Примеры конфигураций
```

---

## 🔧 Ключевые компоненты

### 1. LLM Провайдеры

**Архитектура:** Strategy + Factory паттерн

- **Базовый класс:** `LLMProvider` (ABC)
- **Провайдеры:**
  - `OpenRouterProvider` — OpenRouter API (приоритет #1)
  - `OpenAIProvider` — OpenAI API
  - `DeepSeekProvider` — DeepSeek API
  - `VLLMProvider` — локальный OpenAI-compatible сервер
  - `MockLLMProvider` — для тестирования

**Особенность:** Большинство провайдеров наследуются от `OpenAICompatibleChatProvider`, что устраняет дублирование кода для OpenAI-совместимых API.

### 2. Обработка файлов

**Поток обработки:**
```
MR → Парсинг diff → Фильтрация → Последовательная обработка → Валидация → Отправка
```

- **Парсинг:** Поддержка unified diff формата и обычных файлов
- **Фильтрация:** Игнорирование бинарных файлов и паттернов (glob)
- **Обработка:** Последовательная (с возможностью миграции на параллельную)
- **Chunking:** Для файлов выше лимита контекста (с перекрытием)

### 3. Валидация комментариев

- **Валидатор:** `CommentValidator` с LLM-провайдером
- **Критерии:** Релевантность, полезность, не избыточность
- **Порог:** Настраиваемый (по умолчанию 0.7)
- **Обработка:** Отклонение невалидных с логированием

### 4. Конфигурация

**Иерархия приоритетов:**
1. Дефолтные значения
2. `.ai-reviewer.yml` файл
3. Environment variables
4. CLI аргументы

**Основные секции:**
- `llm` — настройки LLM провайдера
- `validator` — настройки валидации
- `gitlab` — настройки GitLab
- `ignore` — паттерны игнорирования
- `limits` — лимиты обработки
- `comments` — режимы комментариев
- `retry` — настройки retry логики
- `prompts` — кастомные промпты

### 5. GitLab интеграция

- **Клиент:** `GitLabClient` на базе `python-gitlab`
- **Функции:**
  - Получение данных MR
  - Парсинг diff в структурированный формат
  - Отправка inline и summary комментариев
  - Retry логика для API вызовов

---

## 📊 Технический стек

### Основные зависимости

- **Python:** >= 3.10
- **click:** CLI интерфейс
- **pyyaml:** Парсинг конфигурации
- **python-gitlab:** GitLab API клиент
- **requests:** HTTP клиент

### Разработка

- **pytest:** Тестирование
- **black:** Форматирование кода
- **ruff:** Линтинг

---

## ✅ Реализованные возможности (MVP)

- ✅ Автоматический анализ изменений в Merge Request
- ✅ Генерация inline-комментариев к коду
- ✅ Валидация комментариев перед отправкой
- ✅ Поддержка множественных LLM провайдеров (OpenRouter, OpenAI, DeepSeek, VLLM)
- ✅ Гибкая конфигурация через `.ai-reviewer.yml`
- ✅ Фильтрация бинарных и служебных файлов
- ✅ Retry стратегия для устойчивости к ошибкам
- ✅ Поддержка markdown в комментариях
- ✅ Уровни severity (info, warning, error)
- ✅ Режимы комментариев (inline, summary, both)
- ✅ Chunking для больших файлов
- ✅ Docker образ для CI/CD

---

## ⚠️ Известные проблемы и рекомендации по улучшению

### 1. Дублирование retry логики

**Проблема:**
- Retry логика реализована вручную в `http_client.py` (для LLM провайдеров)
- Аналогичная логика дублируется в `GitLabClient._retry_api_call()` (для GitLab API)
- Обе реализации делают одно и то же: экспоненциальный backoff с jitter

**Рекомендация:**
- Использовать библиотеку `tenacity` или `backoff` для унификации retry логики
- Создать единый декоратор/контекстный менеджер для всех API вызовов
- Это упростит код, улучшит тестируемость и добавит больше возможностей (например, retry на основе исключений)

**Пример:**
```python
from tenacity import retry, stop_after_attempt, wait_exponential

@retry(
    stop=stop_after_attempt(3),
    wait=wait_exponential(multiplier=1, min=1, max=10),
    retry=retry_if_exception_type((HTTPError, RequestException))
)
def api_call(...):
    ...
```

### 2. Валидация конфигурации

**Проблема:**
- `ConfigManager` использует простой merge словарей без валидации схемы
- Ошибки конфигурации обнаруживаются только во время выполнения
- Нет типизации конфигурации

**Рекомендация:**
- Использовать `pydantic` для валидации конфигурации
- Определить схемы для всех секций конфига
- Валидация при загрузке конфига (fail-fast подход)
- Автоматическая генерация документации из схем

**Пример:**
```python
from pydantic import BaseModel, Field

class LLMConfig(BaseModel):
    provider: str = Field(..., pattern="^(mock|openrouter|openai|deepseek|vllm)$")
    model: str
    temperature: float = Field(0.7, ge=0.0, le=2.0)
    max_tokens: int = Field(2000, gt=0)
```

### 3. Промпты и шаблонизация

**Текущее состояние:**
- Используются f-strings для шаблонизации промптов
- Это работает, но ограничивает возможности

**Рекомендация (опционально):**
- Рассмотреть миграцию на `Jinja2` для более сложных шаблонов
- Это позволит использовать условную логику, циклы, фильтры
- Но для MVP f-strings достаточно

### 4. Асинхронность

**Текущее состояние:**
- Все HTTP вызовы синхронные (`requests`)
- Обработка файлов последовательная

**Рекомендация (для будущего):**
- Рассмотреть миграцию на `httpx` или `aiohttp` для асинхронных вызовов
- Параллельная обработка файлов ускорит обработку больших MR
- Но это требует рефакторинга всей инфраструктуры

### 5. Тестирование

**Текущее состояние:**
- Есть базовые тесты для ключевых компонентов
- Недостаточно интеграционных тестов

**Рекомендация:**
- Увеличить покрытие тестами
- Добавить моки для GitLab API
- Добавить тесты для edge cases

### 6. Обработка ошибок

**Текущее состояние:**
- Базовая обработка ошибок есть
- Но можно улучшить сообщения об ошибках

**Рекомендация:**
- Создать иерархию кастомных исключений
- Более понятные сообщения об ошибках для пользователей
- Логирование с контекстом

### 7. Документация

**Текущее состояние:**
- Есть README, ADR документы, примеры
- Но можно улучшить API документацию

**Рекомендация:**
- Добавить docstrings для всех публичных методов
- Рассмотреть использование `sphinx` для генерации документации
- Добавить больше примеров использования

---

## 🔄 Миграционные пути

### Параллельная обработка
- Добавить пул потоков/процессов на уровне Application Layer
- Сохранить интерфейсы Domain Layer без изменений
- Использовать `concurrent.futures` или `asyncio`

### Асинхронность
- Заменить `requests` на `httpx` в Infrastructure Layer
- Domain Layer остается без изменений
- Добавить async/await в Application Layer

### Jinja2 для промптов
- Заменить f-strings на Jinja2 шаблоны
- Сохранить обратную совместимость с простыми промптами

---

## 📚 Дополнительные документы

- [README.md](../README.md) - Основная документация
- [QUICK_START.md](../QUICK_START.md) - Быстрый старт
- [ROADMAP.md](../ROADMAP.md) - План разработки
- [ADR документы](ADR/) - Детальные архитектурные решения
- [Рекомендации по валидации](validation-strategy-recommendations.md) - Стратегия валидации

---

## 🎯 Критерии успеха MVP

- ✅ Полный цикл: CLI → GitLab MR → анализ → комментарии
- ✅ Поддержка OpenRouter (и минимум еще один провайдер)
- ✅ Валидация комментариев работает
- ✅ Работает в GitLab CI через Docker
- ✅ Конфигурируется через YAML и CLI
- ✅ Обрабатывает типичные MR (до 20-30 файлов)

**Статус:** Все критерии выполнены ✅

---

## 📝 Следующие шаги

1. **Рефакторинг retry логики** — унификация через библиотеку
2. **Валидация конфигурации** — использование pydantic
3. **Улучшение тестирования** — увеличение покрытия
4. **Оптимизация производительности** — параллельная обработка
5. **Расширение функциональности** — новые провайдеры, улучшения промптов

---

**Последнее обновление:** 2026-01-XX  
**Статус:** MVP завершен, готов к использованию и дальнейшему развитию ✅
